---
id: text_conflict_communication_v2
scoring: answer_weights
questions: 29
factors:
  [style_direct, style_pressure, style_withdraw, style_yield, style_indirect, reactivity, validity_lie]
# Стили считаются только по 12 вопросам-действиям; реактивность — по 12 вопросам-чувствам;
# validity_lie — по 4 L-айтемам. Демографический вопрос пола не скорится.
target_distribution:
  direct_lead: 12-24
  pressure_lead: 8-20
  withdraw_lead: 12-24
  yield_lead: 10-22
  indirect_lead: 8-20
  mixed: 15-30        # осознанный фолбэк: смешанный стиль — честный результат
max_fallback_share: 30
reachability: all
dead_profiles: []
min_items_per_factor: 8   # стили: предложен в ≥8 палитрах из 12; реактивность 12; лжи 4 (валидностная, порог ниже)
scale_contract:
  min_weight_items: 2
free_tier: bars_free
report_kind: scenario
support_note: []
validity:
  lie_factor: validity_lie
  lie_threshold: 70        # % от максимума L-шкалы; калибруется симуляцией
  opt_out_max_share: 0.25  # доля «ничего из этого» по вопросам-действиям
gender_question: g
---

# Намерение

Замена флагмана `text_conflict_communication` по стандарту
[tests-rework-standard.md](../tests-rework-standard.md). Тест отвечает на вопрос «как ты
реагируешь на раздражители в переписке» **двумя осями**, а не одной:

1. **Стиль реакции** — что ты делаешь. Пять стилей по двухосевой модели конфликтного
   поведения (забота о своём интересе × забота об отношениях; классическая рамка
   организационной психологии, без фирменных имён и атрибуций — правовая линия каталога):
   - `style_direct` — **прямой разговор**: своё названо, отношения удержаны (сотрудничество);
   - `style_pressure` — **напор**: своё продавливается за счёт отношений (доминирование);
   - `style_withdraw` — **уход**: ни своё, ни отношения не адресуются (избегание);
   - `style_yield` — **уступка**: отношения ценой своего (приспособление). Новый стиль —
     в v1 «Ну ладно, давай (хотя не хочу)» ошибочно засчитывалось в избегание;
   - `style_indirect` — **обходной манёвр**: сарказм, намёк, пауза-наказание (непрямое
     давление). Наследник passive_revenge из v1.
2. **Реактивность** — насколько раздражитель вообще задевает. Меряется ОТДЕЛЬНЫМИ
   вопросами «что почувствуешь / что подумаешь / насколько это событие», а не постулируется
   контекстом. Это ответ на претензию №3 основателя: «а если меня не триггерит?» — теперь
   «не триггерит» — это измеряемый ответ, а не поломка теста.

Формула результата: ведущий стиль (профиль) × реактивность (видимая шкала). Профили — 5
стилевых + «смешанный». Реактивность вплетается в разборы (фид получает обе оси).

Тест НЕ утверждает диагнозов; стили — стратегии, а не патологии. «Спокойно отвечу — мне
не сложно» — полноценный вариант с нормальным скорингом, а не отсутствующая клетка.

# Конструкция

29 экранов: 1 демографический (пол, `g`, не скорится) + 12 сценариев × 2 вопроса
(чувство → действие) + 4 L-айтема, вставленных между сценариями (позиции ~7, 13, 19, 25).
Ситуация показывается на обоих экранах пары: на экране действия человек уже пролистал
переписку, и вопрос «что сделаешь?» без неё висит в воздухе.

- **Сценарии** — ситуация фактами (без вложенных чувств и интерпретаций), контекст только
  фактический. 12 отношенческих доменов: друг (отмена, ночные сообщения, настойчивые
  планы, пересланное личное), партнёр (молчание онлайн, «ты всегда», сухое «Ок.»),
  группа (шутка про тебя, бытовой чат), семья (тревожные сообщения родителя), работа
  (просьба в 22:00), полузнакомый (в долг).
- **Вопросы-чувства**: три чередующихся формата (палитра чувств / «насколько событие» /
  «что первое подумаешь» — аппрайзалы как скрытая методика). Веса `{reactivity: 0..3}`.
  Палитра НЕ сводится к лестнице «спокоен → взбешён»: в каждой есть и не-конфликтные
  прочтения — облегчение, тревога за другого, вина, узнавание себя («может, и правда я так
  реагирую») — и «не пойму, что чувствую» во всех двенадцати. Порядок не монотонный,
  поэтому вопросы-чувства перемешиваются наравне с действиями.
- **Вопросы-действия**: 5–7 вариантов плюс opt-out, состав меняется от вопроса к вопросу.
  Обязательный минимум палитры: спокойный ход («просто отвечу»), уточняющий («что
  случилось?»), перенос в голос («наберу») и отложенный ответ без наказания — там, где они
  реалистичны. Смешанные действия несут два ключа (`{style_yield: 1, style_indirect: 0.5}`).
  «Ничего из этого не похоже на меня» — во всех двенадцати (`optOut`, не скорится, копится
  в счётчик достоверности, при перемешивании закреплён последним).
- **L-шкала**: 4 айтема, градуированные (верно … неверно), веса `{validity_lie: 0..3}`.
  Два прямых (согласие = приукрашивание) и два обратных (согласие = честное признание):
  односторонняя шкала обнуляется одной стратегией ответов. Темы разведены со стилями теста —
  айтем про скорость ответа пересекался бы со стилем «уход» и давал бы балл за стиль, а не
  за неискренность. Не участвует в профиле и не показывается; выше порога — оговорка о
  достоверности на результате и пометка в фиде.
- **Пол**: `g` первым экраном («В мужском роде / В женском роде / Без разницы»), ответ
  хранится в карте ответов; рендер подставляет формы по инлайн-шаблону `{муж|жен}`.
  Партнёры и третьи лица в ситуациях — без предположений пола и ориентации.

# Скоринг (режим answer_weights)

Ответ несёт карту `weights: {factor: points}`. Фактор-проценты: заработанное/максимум по
вопросам, где фактор ПРЕДЛОЖЕН (у стилей знаменатели различаются — процент читается как
«склонность, когда стиль был доступен»). Ключ присутствует в карте — фактор «выстреливает»
в сквозные шкалы с силой points/max; ключа нет — не выстреливает (стили, которые не выбрал,
не вносят ничего — семантика answer_factor сохранена). `optOut` — вопрос выпадает из
знаменателей, как неотвеченный, но считается отвеченным для полноты.

Выбор профиля: правила на стилевых процентах (реактивность и validity_lie исключены из
ранжирования через `rankOver`): top1 ≥ 38 → именной; top1 ≥ 28 ∧ diff12 ≥ 8 → именной;
иначе `the_mixed`. Пороги калиброваны персонной симуляцией 11.08.2026 (N=8000, двухшаговая
модель выбора): mixed 22.3 / direct 18.7 / indirect 15.8 / yield 15.0 / withdraw 14.3 /
pressure 13.9 — коридоры держатся, мёртвых профилей нет, L-шкала флажит 8.3% сессий.
Отдельно выровнена **доступность стилей**: 11–12 вопросов и знаменатель 9.9–12.0 у каждого,
иначе тест мерил бы палитру, а не человека. Перекалибровка — по первым 200 прод-сессиям.

# Известные компромиссы

- 12 голосов на 5 стилей — грубая сетка; компенсируется вторичными ключами (0.3–0.5) и
  калибровкой diff12. Если симуляция не удержит коридоры — добавлять сценарии, не менять
  семантику.
- Проверки внимательности нет: тест короткий (29 экранов), L-шкала + optOut-счётчик +
  серверный straight-line покрывают достоверность.
- Реактивность в текст профиля не ветвится (профилей было бы 11) — она видимая шкала +
  материал фида. Горячие/холодные варианты профилей — кандидат на v2.1 по данным.
- Слепая считка на прогоне 11.08 всё ещё опознавала «социально одобряемый» вариант чаще
  случайного, хотя уверенная раскладка по стилям была лишь у 7 вопросов из 12. Полностью
  скрыть желательность в сценарном тесте нельзя: человек и так знает, что кричать капсом
  хуже, чем договориться. Ответ на это — не маскировка, а L-шкала: она ловит того, кто
  отвечает «как надо», и помечает результат.

# История изменений порогов/весов

- 11.08.2026 — первая версия спеки (эта переработка — эталон стандарта).
- 11.08.2026, после слепой считки — палитры действий расширены (звонок, уточняющий ход,
  отложенный ответ), палитры чувств перестали быть лестницей, L-шкала стала двусторонней,
  проективные вопросы возвращены к первому лицу, выровнена доступность стилей. Пороги
  40/34/10 → 38/28/8 (перекалибровка на исправленной модели симуляции).
