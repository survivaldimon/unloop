# Э1 — Каталожный триаж: что берём из tests_app

Статус: запускной набор подтверждён основателем 28.07.2026; с 30.07.2026 семь тестов живут на проде (`looplore.app/tests`). Права с партнёром урегулированы (устно, 27.07.2026). **Волна 2 начата 30.07.2026** — батч 1 (`friendship_psychology_v1`, `values_priorities_v1`) задеплоен в прод в тот же день, на `/tests` девять тестов. См. раздел «Волна 2» ниже.

Основание: `tools/tests-import/out/` (извлечение Э0) + выборочный просмотр вопросов и профилей. Итоговые цифры аудита пересчитываются командой из `tools/tests-import/README.md`.

---

## Решение: запускной набор — 7 тестов

| Тест | Вопросов | Профилей | Роль в продукте |
|---|---|---|---|
| `text_conflict_communication` | 20 сценариев | 5 | Флагман: уникальный формат, шеринг |
| `attachment_styles_v1` | 60 | 5 | Ядро ниши, стыкуется с ANX/AVO квиза |
| `friendship_red_flags_v1` | 24 | 4 | Короткий вход, 7 минут |
| `love_languages_v1` | 30 | 14 | Поисковый спрос |
| `toxic_patterns` | 48 | 10 | Самый острый контент, шеринг |
| `ipip_big_five` | 50 | 16 | Якорь доверия, public domain |
| `sixteen_types` | 80 | 16 типов | Массовый спрос, опора сквозного «типа личности» |

312 вопросов, 70 результатов. Диапазон длины 20–80 вопросов закрывает и импульсный вход, и глубокое прохождение.

**Долг по шкалам у этого набора — 53 фантомные шкалы, 162 мёртвые ссылки из 1418 (11%).** Причём 137 из 162 — это один `toxic_patterns`; `friendship_red_flags`, `love_languages`, `ipip_big_five` и `sixteen_types` чисты полностью, у `attachment_styles` четыре ссылки. То есть Э2 — это фактически ревизия одного теста плюс написание весов для `text_conflict_communication`.

### Почему именно эти

`text_conflict_communication` — единственное, чего нет ни у кого: 20 реальных переписок с контекстом («это уже третий раз за месяц»), четыре варианта ответа с эмодзи, живой язык. Это не опросник, это узнавание себя. Лучший кандидат в рекламные креативы.

`attachment_styles_v1` — прямое попадание в то, что Looplore уже измеряет. Шкалы secure/anxious/avoidant/fearful ложатся на ANX/AVO из `src/lib/scoring.ts`, то есть тест не просто добавляется в каталог, а обогащает существующий профиль пользователя.

`friendship_red_flags_v1` — 24 вопроса, семь минут, полностью чистый по аудиту, с обратными пунктами. Идеальный первый тест для холодного трафика.

`love_languages_v1` — самый высокий поисковый спрос в наборе, 14 профилей включая комбинированные.

`toxic_patterns` — контент, от которого невозможно оторваться: тест измеряет **собственную** токсичность пользователя («Я говорю „всё нормально“, когда на самом деле злюсь»). В оригинале он даже не подключён к приложению. Требует самой аккуратной работы с тоном — см. ниже.

`ipip_big_five` — не виральный, но нужен: настоящий инструмент из public domain, с честным дисклеймером в оригинале. Даёт набору научную опору, которой не дадут остальные.

`sixteen_types` — включён по решению основателя, и цифры это поддерживают: ноль фантомных шкал, 80 весов со стопроцентным покрытием, а главное — он основной поставщик биполярных полюсов (E/I, S/N, T/F, J/P) для сквозного «типа личности». Без него SKU «сводный портрет» опирается на разрозненные вклады других тестов и считается заметно хуже. Единственный дорогой пункт — переименование типов, см. правовые заметки.

---

## Что чинить в каждом из шести

| Тест | Работа |
|---|---|
| `text_conflict_communication` | ✅ Веса написаны с нуля (Э4). Э8: эмодзи из названий убраны, заполнены пустые `strengths` у трёх профилей и `vulnerabilities` у ассертивного |
| `attachment_styles_v1` | ✅ Э2 закрыла шкалу (`self_confidence` → алиас), Э8 — голос: канонические термины переехали из названий профилей в текст, у тревожно-избегающего добавлен `supportNote`. Открыто: сокращение с 60 до ~40 вопросов |
| `friendship_red_flags_v1` | ✅ Э8: голос всех четырёх уровней, названия из ярлыков в короткие, `supportNote` у верхнего. Типология вместо уровней остаётся открытой — это правила выбора профиля, а не текст |
| `love_languages_v1` | ✅ Э8: переименован («Как ты считываешь любовь»), отсылок к Чепмену в контенте не осталось, все 14 профилей и подписи факторов в голосе |
| `toxic_patterns` | ✅ Э2 закрыла шкалы, Э8 — тон. `severe_toxicity` переписан целиком (был диагноз «вы — абьюзер», ПТСР у партнёров и совет про медикаменты), добавлен блок поддержки `supportNote`. Подключён в каталог |
| `ipip_big_five` | Ничего по данным. ✅ Э8: 16 профилей в голосе, эмодзи убраны, упоминание IPIP оставлено намеренно |
| `sixteen_types` | ✅ Э8: английские названия и заголовок теста переписаны своими, все 7 секций заполнены по 16 типам. Обсудить сокращение с 80 вопросов |

---

## Вердикты по всем 47

### Запуск (7)
`text_conflict_communication`, `attachment_styles_v1`, `friendship_red_flags_v1`, `love_languages_v1`, `toxic_patterns`, `ipip_big_five`, `sixteen_types`

### Переработать перед взятием (4)

| Тест | Что не так |
|---|---|
| `conflict_communication_style_v1` | Контент сильный (45 бытовых сценариев), но в описании прямо заявлено «основан на модели Томаса-Килманна» — убрать атрибуцию. Веса непригодны: 5 записей на 45 вопросов, и те ссылаются на несуществующие id. Веса с нуля |
| `love_profile` | 60 вопросов, 17 профилей — богато, но факторы названы `BP/IU/ST/OG/DR/KK` без расшифровки, и тест длинный. Расшифровать факторы, решить про сокращение |
| `dark_personality_traits` | Тёмная триада — высокий спрос, но подача «вы нарцисс» неприемлема. Переписать рамку с диагноза на паттерн. Не зарегистрирован в оригинале |
| `self_sabotage_how_you_block_yourself_v1` | Хорошая тема, 24 вопроса, но 47 фантомных шкал на такой объём. Не зарегистрирован |

## Волна 2 — расширение каталога (с 30.07.2026)

Порядок: батчами по 1–2 теста (урок Э8), сначала второй эшелон по ожидаемой ценности, четвёрка «переработать» — последней. Каждый батч проходит весь конвейер: scale-map → правила профиля → канонический JSON → оверрайды Э8 → фикстуры → регистрация (registry, freeTier, обе эдж-функции) → верификация → смоук. Деплой каждого батча — только после «го».

### Батч 1 ✅ в проде (задеплоен 30.07.2026 по «го» основателя)

Деплой: фронт через пуш в main (GitHub Pages CI, коммит `ffc025b`, хеши чанков сверены с локальной сборкой), функции — `npx supabase functions deploy tests-generate-report|tests-portrait --project-ref ncfpxetzmeeqxgqidosj --use-api` (обе теперь v2, `verify_jwt` сохранён, бандлер CLI сам подтянул контент девяти тестов по относительным импортам).

**`friendship_psychology_v1`** («Какой ты друг», 24 q, 13 профилей) — повышен из второго эшелона, как и предполагал триаж. Профили жили в собственном классе `FriendshipProfile` — конвертер научен читать `customProfiles` (маппинг `characteristics`→`strengths`, `inspiringMessage`→`inspiringConclusion`; `suitableRoles` канонического слота не имеет, смыслы вплетены в описания). Правила выбора перенесены из Dart один в один (13 веток, первое правило — «разрыв топ-1/топ-2 < 10% = смешанный»). Scale-map: +2 алиаса, `conscientiousness` заведена новой шкалой (домен Большой пятёрки, 65 ссылок в 13 тестах — пригодится всему второму эшелону). Э8: названия типов из ярлыков в короткие образы (Тёплый эмпат, Мост, Радар, Фейерверк…), `whyThisProfile` написан с нуля под правила. Бары бесплатны (типология: тип — шер-объект, бары — его доказательство).

**`values_priorities_v1`** («Куда уходит твоя энергия», 40 q, 9 профилей) — механика «важность vs энергия» по 10 сферам. Композитные индексы оригинала (средняя важность, средняя энергия, средний |разрыв| по парам) выражены новым генерическим механизмом `derived`-операндов в правилах (`src/tests/types.ts` + `engine.ts`, ~20 строк): любой будущий тест с групповыми факторами обходится данными, движок по-прежнему не знает id тестов. Scale-map: 49 фантомных шкал разнесены (+40 алиасов, 6 новых шкал под будущие тесты выгорания/креативности/веллбинга, 2 в drop). Э8: голос, выброшены англицизмы, у «На износ» (риск выгорания) капс и совет про психолога заменены на спокойный `supportNote`. Бары скрыты — 20 полос это шум, а разрывы важность/вложение и есть платный разбор.

Оба — `spectrum` по типу разбора: новых системных промптов не потребовалось, `tests-generate-report` и `tests-portrait` получили только импорты контента (нужен редеплой функций при выкладке). Верификация: `tests:build` без предупреждений, фикстуры 32/32 (перегенерация подтвердила байт-в-байт воспроизводимость семи прод-тестов), `tsc` чист, прод-сборка собирает оба теста отдельными чанками ~70 КБ, смоук на дев-сервере пройден (оба языка, профили по правилам, граница §2 на экране).

### Батч 2 ✅ в проде (задеплоен 30.07.2026 по «го» основателя)

**`imposter_syndrome`** («Синдром самозванца», 42 q, 5 уровней) — уровневый тест по среднему шести механизмов, пороги 21/41/61/81 как у toxic_patterns. Scale-map: 32 фантомные шкалы разнесены (+28 алиасов, 2 новые шкалы — `fear_of_failure` под perfectionism_fear_of_error_v1 и `social_comparison` под fomo_social_comparison_v1, 2 в drop). Э8 по линии severe_toxicity: исходник на high прописывал «⚠️ настоятельно психотерапевт», на severe — «🚨 НЕОБХОДИМА помощь, психиатр, излечимое состояние»; теперь паттерн и его цена + спокойный `supportNote` на двух верхних уровнях; атрибуция Кланс/Аймс убрана; названия уровней — арка «Шёпот сомнения → Голос в голове → Самозванец за рулём → В тени самозванца». Тип разбора `levels` (внесён в REPORT_KIND — levels не выводится из механики). Бары платные.

**`social_battery_v1`** («Социальная батарея», 48 q, 12 типов) — единственный чистый по аудиту тест эшелона (0 фантомных шкал). Правила: полосы по среднему + ветки по доминирующему фактору (`X >= @top1`). Э8: «Диагностика» убрана из заголовка, типы в метафоре батареи (Вечный заряд, Короткий заряд, Ток с помехами…), у тревожного контура `supportNote` вместо «работайте с терапевтом», два вопроса переведены с «вы» на «ты». Важная находка: факторы `recharge_method` и `interaction_preference` ключёваны в экстравертную сторону (высокое значение = зарядка от людей / тяга к группам) — подписи факторов написаны по фактической ключёвке, а не по названию. Бары бесплатные (шер-крючок «моя батарея садится за час»).

Верификация: фикстуры 40/40, tsc чист, чанки ~60 и ~88 КБ, смоук обоих на дев-сервере (профили по правилам, supportNote рендерится, граница §2 на экране), линт Э8 чистый.

### Батч 3 ✅ в проде (задеплоен 30.07.2026 по «делаем» основателя)

**`boundaries_people_pleasing`** («Личные границы и угождение», 48 q, 3 профиля) — 47 фантомных шкал разнесены (+42 алиаса, 2 новые шкалы: `people_pleasing` и `codependency` — оба вернутся в реляционных тестах, 3 в drop). Правила: две пороговые комбинации + фолбэк-строитель. Э8: англицизм из заголовка, эмодзи из названий («Удобный», «Строитель границ», «Хозяин границ»), у Удобного «работа с психологом» из рекомендаций заменена мягким supportNote. Бары платные.

**`fomo_social_comparison_v1`** («FOMO и чужая идеальная жизнь», 48 q, 5 уровней) — чист по аудиту, но самый тяжёлый контент после severe_toxicity: на верхних уровнях исходник ставил «клиническую зависимость», назначал психиатра «НА СЕГОДНЯ», антидепрессанты, стационар и кризисный телефон, плюс бренды (Instagram, TikTok, книги) и эмодзи в каждой строке. Переписан целиком: арка уровней «Лента — инструмент → Лёгкий крючок → Скролл берёт своё → Лента рулит → Жизнь на ленте», supportNote на двух верхних (без телефонов и назначений), у нижнего уровня — честная оговорка про привычку предельных слов. Тип разбора `levels`. Бары платные.

Верификация: фикстуры 48/48, синтетика подтвердила достижимость boundary_master, tsc чист, чанки 50 и 70 КБ, смоук на дев-сервере, линт Э8 чистый.

### Батч 4 ✅ в проде (задеплоен 30.07.2026 по «делаем» основателя)

**`burnout_diagnostic_v1`** («Выгорание», 54 q, 12 профилей) и **`digital_detox_test`** («Цифровой детокс», 50 q, 12 профилей) — оба чисты по аудиту (0 фантомных шкал: заведённые батчами 1–3 `burnout`, `self_care`, `work_life_balance` уже кормятся). Правила у обоих одной формы: полосы по среднему + доминирующий симптом в средней полосе (порядок правил повторяет порядок обхода факторов Dart — ничьи решаются одинаково); мёртвые default-ветки свитча (`profile_moderate`, `profile_problematic`) стали фолбэками средней полосы. Э8: у выгорания убраны «Диагностика»/ВОЗ/Маслач и рецепты (КПТ, психиатр, медикаменты, больничный) — supportNote на severe/critical + мягкая медицинская строка у соматического; у детокса вычищена рамка «зависимость» и бренды, supportNote на двух верхних. Названия — «Пустой бак», «Броня цинизма», «Тело говорит первым», «Рука сама тянется», «Внимание вдребезги», «Жизнь на экране»… 14 вопросов переведены с «вы» на «ты» (ловушка: `\\w` в JS не матчит кириллицу — «Ваша» ловится только явным списком форм). Бары платные у обоих — разбивка по каналам и есть разбор. Тип `levels` у обоих.

Верификация: фикстуры 56/56, tsc чист, чанки 105 и 94 КБ, смоук с целевыми доминантами (Броня цинизма, Внимание вдребезги) на дев-сервере, линт Э8 чистый.

### Второй эшелон — готовы, но не в первую волну (осталось 12)
`disc_personality_v1`, `holland_code_v1`, `temperament_profile_test`, `creative_type_v1`, `self_confidence_multiscale_v1`, `procrastination_productivity_style_v1`, `romantic_potential_v1`, `relationship_compatibility_v1`, `emotional_intelligence`, `motivational_strategies_v1`, `perfectionism_fear_of_error_v1`, `wellbeing_happiness_inventory_v1`

(Забраны волной 2: батч 1 — `friendship_psychology_v1`, `values_priorities_v1`; батч 2 — `imposter_syndrome`, `social_battery_v1`; батч 3 — `boundaries_people_pleasing`, `fomo_social_comparison_v1`; батч 4 — `burnout_diagnostic_v1`, `digital_detox_test`.)

Заметки: `motivational_strategies_v1` — 90 вопросов, брать только резаным. У `relationship_compatibility_v1` и `romantic_potential_v1` по 3 профиля плюс пофакторные интерпретации. Реально пустые по результатам — `perfectionism_fear_of_error_v1`, `cognitive_ability_v1`, `mental_age_lifespan_styles_v1`: там только названия факторов.

### Не берём (17)

**Клиника и регуляторный риск (4):** `depression_symptoms_inventory_v1`, `anxiety_symptoms_inventory_v1`, `adhd_attention_profile_v2`, `attention_style` (последний — тот же СДВГ-скрининг под названием «стиль внимания»). Health-данные, медицинские заявления в сторах и прямой риск для рекламного кабинета Meta.

**Вне ниши или слабые (12):** `cognitive_ability_v1` (IQ без нормирования, 60 вопросов на 30 минут, результатов нет), `career_compass_v1` (56 парных сравнений, ипсативная механика, карьера), `digital_career_fit_v1` (18 вопросов, вне ниши), `color_psychology_v1` (проективная методика по цветам — развлечение, выдающее себя за диагностику), `visual_micro_tests_v1` (данные собираются кодом, статически не извлекаются; скоринг — простое накопление черт), `fisher_temperament` (Fisher Temperament Inventory под копирайтом), `mental_age_lifespan_styles_v1` (результатов нет), `gaming_balance_check_v1` (аддикция — та же зона риска), `sleep_recovery_v1`, `money_mindset_habits_v1` (вне ниши), `stress_test` и `self_esteem_test` (по 5 вопросов, наследие первого коммита).

---

## Правовые заметки

Формулировки пунктов везде, где смотрел, оригинальные — прямого копирования защищённых опросников не нашёл. Риски не в текстах, а в названиях и атрибуции:

- **`love_languages_v1`** — «Пять языков любви» это бренд Гэри Чепмена. Сама идея (люди по-разному выражают и считывают любовь) не охраняется, пять категорий — тоже описательные, а вот название и прямая отсылка к автору в платном продукте лишние. Переименовать, например «Как ты считываешь любовь».
- **`conflict_communication_style_v1`** — убрать «Thomas-Kilmann» из описания. Названия пяти стилей (избегание, приспособление, соперничество, компромисс, сотрудничество) — общепринятые академические термины, их использовать можно.
- **`fisher_temperament`** — Fisher Temperament Inventory принадлежит Хелен Фишер, не берём в том числе поэтому.
- **`sixteen_types`** — само по себе безопаснее, чем кажется: MBTI и Myers-Briggs в текстах теста не упоминаются ни разу (заявлена опора на типологию Юнга), четырёхбуквенные коды и названия дихотомий общеупотребимы. Проблема в **английских названиях типов**: восемь из шестнадцати дословно совпадают с авторскими прозвищами 16personalities — Architect (INTJ), Defender (ISFJ), Commander (ENTJ), Protagonist (ENFJ), Executive (ESTJ), Consul (ESFJ), Entrepreneur (ESTP), Entertainer (ESFP). Часть остальных (Counselor, Craftsperson) перекликается с системой Кейрси. Английский слой названий переписать целиком своими; русские (Администратор, Мечтатель, Энтузиаст) в основном оригинальны, но лучше пройти тем же проходом. Заголовок «16 Personality Types» тоже сменить — слишком близко к бренду 16Personalities.
- Общее: научную валидность не заявляем нигде. Веса расставлены вручную, нормативных выборок нет. Формулировка — образовательно-развлекательная, как в дисклеймере оригинального IPIP.

---

## Поправка к аудиту Э0

Первая версия аудита считала результатами только конструкторы `TestProfile` и поэтому показывала ноль у девяти тестов. Это было неверно: часть тестов объявляет **собственные классы профилей** (`FriendshipProfile`, `CompatibilityProfile`, `RomanticProfile`, `CareerProfile`), а `sixteen_types` держит описания всех 16 типов markdown-блобами в `getTypeDescriptions()`. Экстрактор дополнен: собирает любые конструкторы с именем на `Profile` и карты интерпретаций, в отчёте появилась колонка `Alt`.

Что изменилось по факту: `sixteen_types` — 16 описаний ru+en (а не ноль), `friendship_psychology_v1` — 13 профилей, `digital_career_fit_v1` — 7, `relationship_compatibility_v1` и `romantic_potential_v1` — по 3. Решения по этим тестам пересмотрены выше.

## Находки, которые меняют Э4

**Фактор может жить в ответе, а не в вопросе.** У `conflict_communication_style_v1` все 45 вопросов имеют `factorId: "multi_choice"`, а стиль закодирован в `score` варианта: 0 = избегание, 1 = приспособление, 2 = соперничество, 3 = компромисс, 4 = сотрудничество. То есть «балл» — это индекс категории, и суммировать его нельзя, надо считать частоты. Так же устроены `digital_career_fit_v1` и восемь вопросов в `creative_type_v1`.

Каноническая схема обязана поддерживать оба способа: маппинг вопрос→фактор и ответ→фактор. Из запускного набора это напрямую касается `text_conflict_communication` (каждый из 4 вариантов = стиль).

**Сценарные тесты — третья форма.** `ScenarioQuestion` = ситуация + контекст + вопрос + варианты. Это не надстройка над обычным вопросом, а другая карточка в UI. Схема должна нести её как тип вопроса, а не как исключение в коде.

---

## Что осознанно отложено

- Сокращение длинных тестов (60 вопросов у `attachment_styles`) — решать на данных о доходимости, не заранее.
- Типологизация профилей уровня риска (`friendship_red_flags`, `toxic_patterns`): уровни хуже шерятся, чем типы, но переделка профилей — это Э8.
- Второй эшелон не размечен по приоритету внутри себя: смысла нет, пока не увидим конверсию первых шести.
